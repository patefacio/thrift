name: thrift
version: 0.0.1
author: Daniel Davidson <dbdavidson@yahoo.com>
dependencies:

# custom <thrift dependencies>
# end <thrift dependencies>

dev_dependencies:
  yaml: "any"
  unittest: ">=0.11.0 < 0.12.0"
  browser: "any"
  hop: "any"
  hop_docgen: "any"

# custom <thrift dev dependencies>
# end <thrift dev dependencies>

dependency_overrides:
# custom <thrift dependency overrides>
# end <thrift dependency overrides>

transformers:

# custom <thrift transformers>
# end <thrift transformers>

